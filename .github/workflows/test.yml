# # .github/workflows/test.yml

# name: CI - Run Vitest Tests

# on:
#   push:
#     branches: [main] # Triggers the workflow on pushes to the main branch
#   pull_request:
#     branches: [main] # Triggers the workflow on pull requests targeting the main branch

# jobs:
#   build-and-test:
#     runs-on: ubuntu-latest

#     strategy:
#       matrix:
#         node-version: [16.x, 18.x] # Test against multiple Node.js versions

#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v3

#       - name: Setup Node.js ${{ matrix.node-version }}
#         uses: actions/setup-node@v3
#         with:
#           node-version: ${{ matrix.node-version }}
#           cache: "npm" # Caches npm dependencies

#       - name: Install Dependencies
#         run: npm install

#       - name: Run Tests
#         run: npm run test -- --coverage

#       - name: Upload Coverage Report
#         uses: actions/upload-artifact@v3
#         with:
#           name: coverage-report
#           path: coverage/
